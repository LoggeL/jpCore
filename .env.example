# Server
PORT=3000
HOST=https://jpcore.logge.top

# CORS - comma-separated allowed origins (leave empty for restrictive default)
CORS_ORIGINS=https://poolparty.jupeters.de,http://localhost:8080

# JWT
JWT_SECRET=change-me-to-a-random-secret
JWT_EXPIRY=7d

# Crypto - pbkdf2 iterations (minimum 100000 recommended)
CRYPTO_ITERATIONS=100000
